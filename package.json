{
  "name": "car-sales-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "postinstall": "prisma generate",
    "vercel-build": "prisma generate && next build"
  },
  "dependencies": {
    // Befintliga beroenden...
  },
  "devDependencies": {
    // Befintliga dev-beroenden...
  },
  "engines": {
    "node": ">=16.0.0"
  }
} 